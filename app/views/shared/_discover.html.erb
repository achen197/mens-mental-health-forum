    <div class="column is-12">
      <h3 class="title is-12">Discover</h3>
      <div class="discussions discover">
        <% @discussions.each do |discussion| %>
          <div class="columns discussion-container discover-card pv10">
            <div class="column is-12">
              <%= link_to discussion do %>
                <h3 class="title discover-title is-12"><%= discussion.title %></h3>
              <% end %>
            </div>

            <% if discussion_author(discussion) || has_role?(:admin) %>
            <div class="column has-text-right">
              <%= link_to 'Edit', edit_discussion_path(discussion), class:'button' %>
              <%= link_to 'Delete', discussion, method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %>
            </div>
            <% end %>

          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="img-container">
  <img src="https://i.imgur.com/8f863j2.jpg" alt="Snow" style="width:20%;">
  <div class="bottom-left">Bottom Left</div>
  <div class="top-left">Top Left</div>
  <div class="top-right">Top Right</div>
  <div class="bottom-right">Bottom Right</div>
  <div class="centered">Centered</div>
